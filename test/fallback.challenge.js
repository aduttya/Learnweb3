// const { expect } = require("chai");
// const { ethers } = require("hardhat");

// describe('Fallback', () =>{

//     let fallback, deployer, attacker

//     let INIT_ETHER = ethers.utils.parseEther('1000')

//     before('deployment',async() =>{

//       // deploy contract with 100 ethers
//       let Fallback = await ethers.getContractFactory('Fallback');
//       fallback = await Fallback.deploy();

//       [deployer,attacker] = await ethers.getSigners()
//     })

//     it('attack',async() =>{

//       // send plain ethers to contract from 
//     })

//     after(async () => {

//     })



// })
